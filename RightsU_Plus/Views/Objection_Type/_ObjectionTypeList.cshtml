@model List<RightsU_Entities.Objection_Type>
@{
    MessageKey objMessageKey = new MessageKey();
    if (Session["objMessageKey"] != null)
    {
        objMessageKey = (MessageKey)Session["objMessageKey"];
    }
}           
<script>
    $('.expand_Opposition_Name').expander({
        slicePoint: 90,
        expandPrefix: '',
        expandText: '...@objMessageKey.readmore',
        collapseTimer: 0,
        userCollapseText: '<span>[^]</span>'
    });

   
</script>
<table class="table table-bordered table-hover" id="tblGenres">
    <thead>
        <tr>
            <th style="width:60%;">Objection Type</th>
            <th style="width:20%;">@objMessageKey.Status</th>
            <th style="width:20%;">@objMessageKey.Action</th>
        </tr>
    </thead>
    <tbody>
        @if (ViewBag.CommandName == "ADD")
        {
            <tr>
                <td>
                    <input id="txtObjectionTypeName_0" type="text" value="" maxlength="100" />
                </td>
                <td align="center">
                    <span>@objMessageKey.Active</span>
                </td>
                <td align="center">
                    <a title="@objMessageKey.Save" class="glyphicon glyphicon-ok-circle" onclick="SaveObjectionType(0)"></a>
                    <a title="@objMessageKey.Cancel" class="glyphicon glyphicon-remove-circle" onclick="Cancel()"></a>
                </td>
            </tr>
        }
        @if (Model.Count > 0)
        {
            foreach (var item in Model)
            {
                string strActive =objMessageKey.Active;
                if (item.Is_Active == "N")
                {
                    strActive = @objMessageKey.Deactive;
                }
                <tr>
                    @if (ViewBag.CommandName == "EDIT" && ViewBag.Objection_Type_Code == item.Objection_Type_Code)
                    {
                        <td>
                            <input id="txtObjectionTypeName_@item.Objection_Type_Code" type="text" value="@item.Objection_Type_Name" maxlength="100" />
                        </td>
                        <td align="center">
                            <span>@strActive</span>
                        </td>
                        <td align="center">
                            <a title="@objMessageKey.Save" class="glyphicon glyphicon-ok-circle" onclick="SaveObjectionType(@item.Objection_Type_Code)"></a>
                            <a title="@objMessageKey.Cancel" class="glyphicon glyphicon-remove-circle" onclick="Cancel()"></a>
                        </td>
                    }
                    else
                    {
                        <td>
                            <div class="expand_Opposition_Name">@item.Objection_Type_Name</div>
                        </td>
                        <td align="center">
                            <span>@strActive</span>
                        </td>
                        <td align="center">
                            @if (ViewBag.UserModuleRights.Contains("~" + UTOFrameWork.FrameworkClasses.GlobalParams.RightCodeForEdit + "~"))
                            {
                                if (item.Is_Active == "Y")
                                {
                                    <a title="@objMessageKey.Edit" class="glyphicon glyphicon-pencil" style="cursor:pointer;" onclick="CheckRecordLock(@item.Objection_Type_Code, 'EDIT')"></a>
                                }
                                else
                                {
                                    <a title="Can not edit this opposition type as it is deactivated" class="glyphicon glyphicon-pencil disabled" onclick="return false;"></a>
                                }
                            }
                            @if (item.Is_Active == "Y")
                            {
                                bool canDeactivate = true;
                                string strToolTip = "";
                                //if (item.Channels.Count > 0)
                                //{
                                //    canDeactivate = false;
                                //    strToolTip = objMessageKey.Cannotdeactivatethisgenreasitisusedinchannel;
                                //}
                                //else if (item.Title_Geners.Count > 0)
                                //{
                                //    canDeactivate = false;
                                //    strToolTip = objMessageKey.Cannotdeactivatethisgenreasitisusedintitle;
                                //}
                                if (canDeactivate)
                                {
                                    <a title="@objMessageKey.Deactivate" class="glyphicon glyphicon-thumbs-down" style="cursor:pointer" onclick="ActiveDeactiveObjectionType(@item.Objection_Type_Code, 'N')"></a>
                                }
                                else
                                {
                                    <a title="@strToolTip" class="glyphicon glyphicon-thumbs-down disabled" onclick="return false;"></a>
                                }
                            }
                            else
                            {
                                if (ViewBag.UserModuleRights.Contains("~" + UTOFrameWork.FrameworkClasses.GlobalParams.RightCodeForActivate + "~"))
                                {
                                    <a title="@objMessageKey.Activate" class="glyphicon glyphicon-thumbs-up" style="cursor: pointer;" onclick="ActiveDeactiveObjectionType(@item.Objection_Type_Code, 'Y')"></a>
                                }
                            }
                        </td>
                    }
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3">@objMessageKey.Norecordfound</td>
            </tr>
        }
    </tbody>
</table>
