
@*<script>
   $(document).ready(function () {
        $("#lblRecordCount").text(@ViewBag.RecordCount);
        $("#hdnRecordCount").val(@ViewBag.RecordCount);
        SetPaging();
   });

 </script>
  <table class=" table table-bordered table-hover">
    <thead>
        <tr>
            <th style="width:20%;">Show Name</th>
            <th style="width:20%;">Music Track Name</th>
            <th style="width:20%;">Movie/Album</th>
            <th style="width:15%;">Music Language</th>
            <th style="width:25%;">Year of Release</th>
        </tr>
    </thead>

    <tbody>
        @if (Model.Count > 0)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@((item.TitleName ?? "NA"))</td>
                    <td>@item.Music_Title.Music_Title_Name</td>
                    <td>@((item.MovieAlbum ?? "NA"))</td>
                    <td>NA</td>
                    <td align="center">0</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5">No record found</td>
            </tr>
        }
    </tbody>
</table>*@

          