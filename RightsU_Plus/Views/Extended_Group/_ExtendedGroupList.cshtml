@model List<RightsU_Entities.Extended_Group>

@{
    MessageKey objMessageKey = new MessageKey();
    if (Session["objMessageKey"] != null)
    {
        objMessageKey = (MessageKey)Session["objMessageKey"];
    }
}

@{
    int i = 0;
}

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>
                Sr. No.
            </th>
            <th>
                Module
            </th>
            <th>
                Group Name
            </th>
            <th>
                Short_Name
            </th>
            <th>
                Group_Order
            </th>
            <th>
                Type
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            foreach (var item in Model)
            {
                i++;
                <tr>
                    <td style="text-align:center">
                        @i
                    </td>
                    <td style="text-align:center">
                        @if (item.Module_Code == 27)
                        {
                            <span>Title</span>
                        }
                        else if (item.Module_Code == 10)
                        {
                            <span>Party</span>
                        }
                    </td>
                    <td style="text-align:center">
                        @Html.DisplayFor(modelItem => item.Group_Name)
                    </td>
                    <td style="text-align:center">
                        @Html.DisplayFor(modelItem => item.Short_Name)
                    </td>
                    <td style="text-align:center">
                        @Html.DisplayFor(modelItem => item.Group_Order)
                    </td>
                    <td style="text-align:center">
                        @Html.DisplayFor(modelItem => item.Add_Edit_Type)
                    </td>
                    <td style="text-align:center">
                        <a title="@objMessageKey.Edit" class="glyphicon glyphicon-pencil" style="cursor: pointer;" onclick="AddEditViewExtdGrp(@item.Extended_Group_Code, 'EDIT')"></a>
                        <a title="@objMessageKey.View" class="glyphicon glyphicon-eye-open" style="cursor: pointer;" onclick="AddEditViewExtdGrp(@item.Extended_Group_Code, 'VIEW')"></a>
                        <a title="@objMessageKey.Delete" class="glyphicon glyphicon-thumbs-up" style="cursor: pointer;" onclick="DeleteExtdGrp(@item.Extended_Group_Code, 'DELETE')"></a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="7">@objMessageKey.Norecordfound</td>
            </tr>
        }
    </tbody>
</table>
